# Copiar este archivo a .env y completar solo si se dispone de las credenciales.
# El pipeline funciona sin estas variables usando modelos locales de Hugging Face
# (ver tarea1_rag_normativo/config.yaml, seccion "embeddings" y "generation").
#
# ALTERNATIVA MAS SEGURA que este archivo: guardar la key en el llavero del
# sistema operativo en vez de en texto plano aqui. Correr UNA VEZ, en tu
# propia terminal:
#
#     keyring set hw03_openai OPENAI_API_KEY
#
# (abre un prompt oculto, la key nunca queda en ningun archivo). El
# proyecto la detecta automaticamente -- ver tarea1_rag_normativo/src/secrets_store.py.

OPENAI_API_KEY=
